FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY . .
RUN node -e "require('fs').mkdirSync('public', { recursive: true })"

EXPOSE 5000

CMD ["node", "server.js"]
